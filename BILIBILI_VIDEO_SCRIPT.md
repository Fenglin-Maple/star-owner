# 《星藏家：把 B 站收藏夹变成 AI 知识库》

> 预计时长：约 6 分 30 秒
> 用途：哔哩哔哩项目介绍与基础使用教程
> 生成语音时，只朗读“配音”部分；“画面提示”不需要朗读。

## 00:00 - 开场

**配音：**

大家好，我是枫林，今天给大家介绍我的一个开源项目，能够把你的 B 站收藏夹变成 AI 知识库。

这个项目叫做“星藏家”。

如果你的收藏夹里也堆了很多 AI 新闻、技术教程和经验分享，但一直没有时间看完，那么星藏家可以自动整理这些视频，把它们变成可以阅读、搜索和继续提问的 Markdown 知识库。

**画面提示：**

OC 开心登场，随后显示星藏家图标、项目名称和应用主界面。

## 00:25 - 先看工作流效果

**配音：**

在介绍安装方法之前，我们先看看它实际运行起来是什么效果。

这里我创建了多个 Agent 视频总结工作流，让它们同时处理收藏夹中的不同视频。

应用会自动准备视频、提取字幕、运行语音识别、抽取关键帧，再把这些材料交给 AI。

上方可以看到整个收藏夹的完成进度，每个 Agent 也会显示自己正在处理的视频和当前工作阶段。

**画面提示：**

展示多个 Agent 工作流。状态依次出现“准备素材”“ASR”“生成文档”等内容，收藏夹总进度条逐渐上升。可以使用多个 OC 分身表现并行工作。

## 00:55 - 展示生成的文档

**配音：**

当一个视频处理完成后，它会自动生成一份 Markdown 文档。

文档里不只有几句话的摘要，还包含小结、思维导图、可点击目录、完整正文、关键帧、字幕比较和视频时间轴链接。

点击时间轴链接，就可以使用默认浏览器直接跳转到原视频的对应位置，方便查看原始内容。

**画面提示：**

快速滚动展示一份完成的 Markdown，重点放大思维导图、目录、关键帧和时间轴链接。

## 01:20 - 展示 RAG 助手效果

**配音：**

当这些文档积累起来以后，就可以交给内置的 RAG 知识库助手。

例如，我可以选择刚刚处理好的收藏夹，然后问它：

“帮我总结这个收藏夹最近出现了哪些值得关注的新技术。”

RAG 助手会搜索多份视频文档，读取原始 Markdown 和相关图片，再整理出带有来源依据的回答。

还可以继续追问、比较不同视频的观点，或者让它根据整个收藏夹整理一套学习路线。

**画面提示：**

选择知识库，输入问题，展示模型思考动画和流式回答。回答中出现视频标题、BV 号和引用来源。

## 01:50 - 从头演示基础流程

**配音：**

这就是星藏家最终想实现的效果：

把不断增长的 B 站收藏夹，变成能够被 AI 阅读和使用的个人知识库。

接下来，我从下载开始，演示一次最基础的使用流程。

第一次使用不需要先弄懂所有技术细节。

整个过程可以简单理解为：

下载星藏家、配置 AI 模型、登录 B 站、同步收藏夹、创建 Agent 工作流，最后在文档库和 RAG 助手中查看结果。

**画面提示：**

显示“下载应用、配置模型、登录 B 站、同步收藏夹、创建工作流、使用知识库”六步流程图。

## 02:15 - 下载 Core 核心包

**配音：**

首先打开星藏家的 GitHub 项目，在 Releases 页面下载最新的 Core 核心包。

文件名类似：

Star Owner，版本号，Windows x64 Core。

下载完成后，把 ZIP 完整解压到一个正常文件夹。不要直接在压缩包预览窗口中运行。

然后双击根目录里的 Start StarOwner 启动文件，星藏家就会打开。

普通用户不需要提前安装 Node.js、Python、FFmpeg、SQLite 或 faster-whisper，这些运行环境都由项目自己管理。

**画面提示：**

展示 GitHub Release 页面，下载 Core ZIP，完整解压，然后双击 `Start-StarOwner.cmd`。

## 02:50 - 首次启动与 ASR 模型

**配音：**

第一次启动时，星藏家会检测运行环境和 ASR 语音识别模型。

如果提示缺少 small 或 medium 模型，点击“同意并开始下载”即可。

下载支持自动重试、断点续传和文件校验。

默认推荐使用多语言 medium 模型。电脑配置较低时，也可以在设置中切换到更轻量的 small。

安装完成后不需要强制重启。工具状态刷新为在线，就可以继续使用。

**画面提示：**

展示首次启动提示、模型下载进度和工具在线状态。OC 从等待表情变为开心表情。

## 03:20 - 配置 AI 模型

**配音：**

接下来进入“AI 模型配置”。

添加一个 OpenAI 或 NewAPI 兼容的模型供应商，填写 Base URL 和 API Key。

然后拉取远程模型列表，选择需要使用的模型并保存。

这里配置的 AI，负责阅读字幕、理解关键帧并生成最终的 Markdown 文档。

应用支持保存多个供应商和多个模型，不同的 Agent 工作流也可以选择不同模型。

**画面提示：**

展示供应商配置、拉取模型列表和保存操作。Base URL 和 API Key 等敏感信息需要打码。

## 03:50 - 扫码登录 B 站

**配音：**

模型配置完成后，进入“B 站登录”。

点击扫码登录，然后使用手机哔哩哔哩客户端完成扫码。

登录成功后，星藏家会自动同步用户名、头像和收藏夹列表。

它使用独立、持久化的登录窗口，不会直接读取平时使用的系统浏览器账号。

**画面提示：**

展示扫码登录过程。扫码内容需要遮挡，登录完成后展示右上角用户名和头像。

## 04:15 - 同步收藏夹与检查任务

**配音：**

进入“收藏夹同步”，选择需要整理的收藏夹，然后点击“同步任务”。

星藏家会读取收藏夹中的视频，并保存 BV 号、标题、UP 主、时长、发布日期和收藏日期等信息。

同步完成后，进入“任务总览”。

这里可以看到待处理、处理中、已完成、失败和已关闭的视频数量。

不需要总结的视频，可以直接关闭。关闭任务不会影响已经生成的文档。

**画面提示：**

展示收藏夹选择、同步进度条、任务状态统计、搜索筛选和任务开关。

## 04:50 - 创建 Agent 视频总结工作流

**配音：**

准备好任务以后，进入“Agent 视频总结工作流”。

创建一个新的 Agent 会话，选择刚才配置的供应商、模型和需要处理的收藏夹，然后开始工作。

星藏家支持同时创建多个 Agent 工作流，让它们并行处理不同的视频。

每个视频都会获得独立的任务和全新的模型上下文，不会把上一个视频的对话混入下一个视频。

如果任务被停止、应用关闭或者处理失败，本次产生的临时文件会被清理，普通任务会回到待处理状态，下次从头开始。

**画面提示：**

创建工作流并选择收藏夹。随后展示多个工作流同时运行，OC 分身分别处理不同任务。

## 05:25 - 视频素材如何处理

**配音：**

任务开始后，应用会自动准备总结需要的素材。

包括下载和合轨视频、提取 B 站字幕、运行 faster-whisper ASR、抽取关键帧，以及获取热评前三条。

即使视频已经有站内字幕，也会再运行一次 ASR。

AI 会比较两份字幕的完整性、术语和时间轴，必要时再结合关键帧判断哪一份更加可靠。

视频下载、FFmpeg、ASR 和缓存清理都由应用统一调度，并负责显存、并发、排队和错误处理。

**画面提示：**

依次展示下载、合轨、字幕、ASR、关键帧、热评和缓存清理状态。

## 05:58 - 文档库与 RAG 助手

**配音：**

任务完成后，生成的文档会自动进入文档库。

可以按照用户、收藏夹、BV 号、标题、UP 主、日期和视频时长进行筛选和预览。

进入 RAG 知识库助手以后，创建一个会话，选择 AI 模型，再勾选需要查询的用户和收藏夹。

之后就可以针对一个收藏夹或者多个知识库连续提问。

除了内置助手，星藏家还提供本机只读知识库接口，可以让 Codex、Claude Code、OpenCode 等外部 Agent 读取文档、元数据和图片资源。

**画面提示：**

展示文档库筛选、Markdown 预览、RAG 知识库多选框，以及外部知识库 API 的启动页介绍。

## 06:30 - 其它功能与版本边界

**配音：**

除了收藏夹工作流，星藏家还提供单视频总结、视频下载缓存、视频库和文档导出等功能。

数据库、字幕、图片、缓存和最终文档，默认保存在应用管理的 Workspace 中，方便备份和统一管理。

目前稳定版主要支持普通 BV 单 P 视频。

多 P 视频，以及番剧、电影、课程和直播等特殊页面，当前版本会明确拒绝处理，避免只生成一部分内容，却被误认为是完整总结。

**画面提示：**

快速切换单视频总结、下载视频、视频库、文档库和导出栏目，随后显示当前版本边界。

## 06:55 - 开源项目与结尾

**配音：**

星藏家基于多个优秀的开源项目构建。

包括 Electron、FFmpeg、yt-dlp、faster-whisper、CTranslate2、Mermaid、sql.js 和 markdown-it。

项目本身也已经在 GitHub 开源。

你可以直接下载使用，也可以查看源码、提交建议、反馈问题，或者按照自己的想法继续迭代和修改，做出更适合自己的视频知识管理工具。

我是枫林，感谢大家观看。

**画面提示：**

依次展示开源项目名称和图标，最后显示星藏家 GitHub 地址、GPL 开源协议、项目图标以及：

`Built with OpenAI Codex.`

---

# 画面开源项目列表

| 项目 | 主要用途 |
|---|---|
| [Electron](https://github.com/electron/electron) | 桌面应用和 WebView 界面 |
| [FFmpeg](https://github.com/FFmpeg/FFmpeg) | 音视频处理、合轨和关键帧提取 |
| [yt-dlp](https://github.com/yt-dlp/yt-dlp) | 视频与媒体资源下载 |
| [faster-whisper](https://github.com/SYSTRAN/faster-whisper) | 本地语音识别和时间轴字幕 |
| [CTranslate2](https://github.com/OpenNMT/CTranslate2) | CPU 与 CUDA 推理运行时 |
| [Mermaid](https://github.com/mermaid-js/mermaid) | Markdown 思维导图 |
| [sql.js](https://github.com/sql-js/sql.js) | 本地 SQLite 数据持久化 |
| [markdown-it](https://github.com/markdown-it/markdown-it) | Markdown 内容渲染 |

# 项目地址

[https://github.com/Fenglin-Maple/star-owner](https://github.com/Fenglin-Maple/star-owner)
