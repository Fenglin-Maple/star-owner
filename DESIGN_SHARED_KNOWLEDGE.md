# GitHub 共享 B 站视频总结知识库设计

> 状态：已接入 `1.4.10` 的功能设计与实现基线；本文同时记录默认/自定义共享仓库、上传事务、共享挂载以及仓库维护者需要遵守的目录、审核和 GitHub Actions 约束。
>
> 第一版范围：只允许上传应用已经完成的 Bilibili 视频总结产物。暂不允许上传本地视频、本地音频、本地文档、共享用户已经导入的文档、原始视频或过程缓存。

标准单 P 总结和标准多 P 父任务总结都属于 Bilibili 视频总结来源。多 P 总结以父任务为共享单位，不单独上传孤立的 P 子文档。

## 1. 目标

在不要求普通用户学习 Git/GitHub 的情况下：

1. 上传者从应用中筛选并多选已完成的 B 站视频总结文档。
2. 应用调用系统默认浏览器引导用户登录 GitHub 并授权，自动 Fork 共享仓库、创建分支、提交文件并创建 Pull Request。
3. 维护者通过 GitHub 审核 Pull Request；GitHub Actions 执行结构、路径、内容和安全检查。
4. 合并到主分支后的文档进入公共目录，下载者可以浏览、过滤、多选并导入应用。
5. 导入的文档挂载到本地“共享”用户下，和“内置用户”、B 站账户处于同一级别。
6. 共享文档可以进入文档库和 RAG 助手，但不进入任务总览，也不能被 Agent 工作流领取。

主分支是远程共享知识库的唯一可见来源。未审核或未合并的 PR 不进入下载目录。

## 1.1 默认与自定义共享仓库

应用默认使用以下 GitHub 仓库：

- 仓库：`Fenglin-Maple/Blibili-Markdowns`
- 地址：https://github.com/Fenglin-Maple/Blibili-Markdowns
- 默认目标分支：`main`
- 当前状态：`main` 已初始化 README、共享目录契约和校验 Actions；应用可以创建贡献分支/PR，只有审核合并到 `main` 的内容才进入下载者目录。

用户可以切换并持久保存其它有权访问的 GitHub 共享仓库，公开仓库允许匿名浏览，私有仓库读取与上传都必须使用星藏家自身保存的有效授权。切换当前仓库只改变后续浏览和上传目标；既有本地挂载保存各自的仓库 owner、name、branch 身份，不会被重定向到新仓库。

用户也可以一键在当前授权 GitHub 账户下创建公开共享仓库。应用创建仓库后，根据 GitHub 实际返回的默认分支预置 `_star-owner-repository.json`、README、贡献与安全说明、CODEOWNERS、PR 模板、仓库文件白名单、PR 校验 Action 和目录索引 Action，然后把它设为当前共享仓库。应用实现不应把仓库名称、分支名称或目录结构硬编码在渲染层，应由共享知识库配置和后端校验共同维护。

## 2. 第一版上传范围

上传面板只展示同时满足以下条件的记录：

- 任务状态为已完成；
- 存在有效 BVID；
- 存在可读取的最终 Markdown；
- 产物来源是 Bilibili 视频总结，而不是本地媒体或本地文档导入；
- 不是“共享”用户下导入的远程文档；
- 不包含本地绝对路径、Cookie、API Key、SQLite、日志或临时文件。

上传内容以最终知识文档为中心，默认包括：

- UTF-8 Markdown 总结正文；
- Markdown 引用的图片、封面或关键帧资源；
- 脱敏后的文档元数据；
- 文档和资源的 SHA-256 哈希。

单视频本地最终 Markdown 无论原文件名多长，进入共享包时都规范为 `summary.md`；多 P 父任务规范为 `index.md`，每 P 正文规范为 `parts/cid-<cid>/summary.md`。`entryMarkdown`、`contentSha256`、`markdownSha256`、`assetSha256` 和 `files` 共同定义入口、正文与资源，Action、下载挂载和 RAG 必须按同一契约读取。

默认不上传原始视频、音频、Agent 草稿、下载字幕缓存、ASR 原始过程文件、评论/字幕过程 JSON、FFmpeg 中间产物、完整任务日志或本地任务元数据。共享包只保留规范 Markdown、允许的图片资源和应用生成的 `_star-owner-document.json`。

上传确认页显示文档标题、BVID、UP 主、来源收藏夹、图片数量、文件大小和预计提交大小，并提醒用户确认内容适合公开共享。B 站视频截图、封面和总结文字仍可能涉及版权，应用不能替用户作出版权判断。

## 3. 稳定身份模型

任务 `task.id` 只用于应用内部任务状态，不能作为共享仓库的永久文档身份。共享协议使用独立的 `documentId`。

### 3.1 稳定来源字段

每个远程文档至少保存：

```json
{
  "schemaVersion": 3,
  "sourceType": "bilibili-video-summary",
  "contributorGithubId": "123456789",
  "bilibiliUid": "987654",
  "remoteCollectionId": "collection-id-or-single",
  "bvid": "BV1xxxx",
  "documentId": "doc-<stable-hash>",
  "contentSha256": "...",
  "assetSha256": {
    "assets/cover.jpg": "..."
  }
}
```

第一版的稳定 ID 规则为：

```text
documentId = sha256(
  "v1|github:" + contributorGithubNumericId +
  "|bilibili:" + bilibiliUid +
  "|collection:" + remoteCollectionId +
  "|bvid:" + normalizedBvid
)
```

说明：

- GitHub 用户名不作为身份，使用 GitHub 数字用户 ID；
- B 站用户名不作为身份，使用 B 站 UID；
- 收藏夹名称不作为身份，使用远程收藏夹稳定 ID；
- BVID 是视频身份；
- 同一贡献者、同一 B 站账户、同一远程收藏夹、同一 BVID 的内容变化属于同一个文档更新；
- 不同贡献者、不同收藏夹或不同 B 站账户，即使 BVID 相同，也保留为不同文档；
- `contentSha256` 只用于判断内容是否变化，不参与 `documentId`，因此重新总结后可以更新原文档；
- Git 历史保留旧版本，不在当前目录中建立历史版本副本。

单视频总结如果没有真实远程收藏夹，使用固定的 `remoteCollectionId=single:<stable-internal-collection-id>` 命名空间，并在元数据中标记“单视频来源”。其中 `stable-internal-collection-id` 是应用为“内置单视频总结收藏夹 + 实际内置收藏夹”保存的稳定 ID，实际显示名称只写入元数据，不作为身份。它可以和 B 站收藏夹来源统一挂在同一个 B 站账户来源下，但不参与真实 B 站收藏夹目录的增量同步。

单视频产物必须能够确认来源 B 站 UID 才能进入上传候选列表。若历史任务或当前任务没有可确认的 B 站 UID，应用应提示用户先选择/绑定来源账户，不能使用用户名、登录 Cookie 或本地收藏夹名称猜测 UID，也不能生成来源不完整的共享文档。

### 3.2 显示名称与目录身份分离

用户名、收藏夹名、标题只用于显示或写入元数据。它们可能改名、包含 Windows/Git 不安全字符或造成路径过长，因此不能直接作为唯一目录名。

## 4. GitHub 仓库目录

共享协议使用稳定 ID 直接组织目录。不会在仓库中写入用户可变的显示名称，也不会依赖 `collection.json` 或 `manifest.json` 完成文档身份判断。目标分支 Action 根据已经合并的文档元数据生成只读 `catalog.json`，应用优先一次读取并校验该索引；缺失、损坏或 schema 不兼容时才回退目标分支 Git tree。实际导入仍会校验快照中的 `_star-owner-document.json`、路径、类型、大小和入口文件，目录索引不能绕过文档安全契约。实际目录为：

```text
<github-numeric-id>/
  bilibili|single|multipart/
    col-<source-collection-hash>/
      doc-<stable-document-hash>/
        summary.md
        _star-owner-document.json
        metadata.json
        assets/
          cover.jpg
          frame-001.jpg
```

实际规则：

- 第一层是上传者的 GitHub 数字 ID，上传者只能写入自己的第一层目录；
- `bilibili` 表示 B 站收藏夹来源，`single` 表示单视频总结来源，`multipart` 表示多 P 父任务来源；
- `col-<hash>` 由来源类型和应用内稳定收藏夹 ID 得到，收藏夹改名不会改变它；
- `doc-<hash>` 是 `documentId`，由 GitHub 数字 ID、B 站 UID、来源收藏夹命名空间和 BVID 计算，不包含本地任务 ID 或显示名称；
- 一个文档的 Markdown、元数据和图片资源必须位于同一个文档目录；
- 文件路径只使用安全 ASCII ID 和受控扩展名，避免特殊字符、路径穿越和超长路径。

每个文档目录中的 `_star-owner-document.json` 是远程目录发现和导入的入口元数据。它包含 `schemaVersion`、`documentId`、来源类型、BVID、标题、B 站 UID、来源收藏夹名称、更新时间、文件清单、文件大小以及 SHA-256。Markdown 优先使用普通视频的 `summary.md`，多 P 父任务优先使用 `index.md`；多 P 的每个 P 保留在 `parts/cid-<cid>/summary.md`。

单视频和多 P 产物都直接挂在贡献者 GitHub 数字 ID 下，不放入随机的 B 站用户目录。B 站 UID、用户名、收藏夹名称和内置收藏夹来源写入元数据，仅用于列表显示和筛选。

## 5. 上传者流程

### 5.1 面板筛选

上传面板支持按以下字段筛选：

- B 站账户；
- 收藏夹；
- 标题；
- BVID；
- UP 主；
- 投稿时间和收藏时间；
- 总结完成时间；
- 已上传、待上传、远程已有更新等状态。

上传候选列表支持按用户、收藏夹、标题/BVID/UP 主、生成时间和视频时长筛选，并可一次全选当前筛选结果。选中的文档进入固定高度、内部滚动的独立准备上传列表，默认只显示涉及的收藏夹，展开后显示每篇文档。准备列表自身还支持按收藏夹、标题/BVID/UP 主继续筛选、全选当前结果、批量移除当前筛选结果和逐项移除，不会因为文档数量增加而拉长整个工具页面。

一次上传最多 `1000` 篇文档，同时总大小不得超过 `1 GiB`；单文件、单文档和文件数量仍受更小的独立上限约束。应用在提交前执行本地预检：文件存在性、Markdown 图片引用、文件大小、路径边界、元数据完整性和是否为共享来源。通过预检的资源以 `sourcePath` 流式复制到项目内置 Git 工作目录，不能把全部文档资源同时保存在 Renderer 或主进程内存中。

上传开始后，应用用全窗口独占模态层夺取输入焦点，持续显示当前阶段、文档/文件进度和总进度，禁止点击导航或其它功能。模态层必须保留“中止上传”按钮；手动中止、网络中断或 Git 失败会终止相关 GitHub 请求和内置 Git 子进程，清理可识别的临时分支、临时 checkout 和当前未完成状态，已经存在的本地总结产物不受影响。

### 5.2 自动 Fork 和 PR

选择或创建目标仓库后：

1. 用户点击“浏览器登录 GitHub”，应用通过项目自带 Git Credential Manager 调用系统默认浏览器；
2. GCM 把浏览器授权结果写入星藏家项目目录内专用的 DPAPI 加密凭据存储，不使用 Windows 全局凭据库；
3. 主进程调用 GitHub `/user` 校验令牌，读取并加密保存必要的登录名、稳定数字 ID 和令牌，不接收或保存浏览器 Cookie；
4. Fine-grained Token 粘贴保留在“更多授权选项”中作为备用路径；
5. 主进程读取目标仓库的 GitHub 数字 owner ID、实际默认/配置分支和可见性；私有仓库没有有效授权时立即拒绝；
6. 当前 GitHub 数字 ID 与仓库 owner ID 一致时直接在上游创建临时分支；否则自动创建或复用父仓库匹配的合法 Fork，若同名仓库不是目标 Fork，则停止并提示重命名；
7. 使用项目自带 Git 环境提交选中文档和 manifest，commit author 使用授权账户的 GitHub noreply 身份；
8. 自动创建指向目标仓库配置分支的 Pull Request；
9. 显示 PR 地址、提交数量和后续审核状态。

用户不需要执行 clone、checkout、commit 或 push。

GitHub 登录不嵌入应用 WebView，也不复制浏览器 Cookie。用户可以清除星藏家数据库和内置 Git 私有 DPAPI 存储中的授权以切换 PR 账户；该操作绝不调用系统 GCM logout/erase，也不清除 Windows Credential Manager、系统 Git 或用户全局 Git 配置。旧版本可能写入系统凭据库的历史记录不会由应用擅自删除，新版本也不会读取或写入该位置。

### 5.3 项目自带 Git 环境

应用必须随核心包提供可验证版本的便携 Git 运行环境，例如：

```text
runtime/
  git/
    cmd/git.exe
    ...
```

所有上传、Fork 工作区准备、分支创建、提交、推送、下载和同步所需的 Git 命令都必须使用该目录中的绝对路径。应用不得调用系统 `PATH` 中的 `git`，也不得因为内置 Git 缺失而静默回退到用户全局 Git。

Git 子进程使用受控环境：

- 设置明确的 `cwd` 和项目 Git 路径；
- 禁用系统级和用户级 Git 配置读取，使用应用自己的临时 Git 配置目录；
- 使用一次性进程环境中的 `http.extraHeader` 传递 Token，不把 Token 写入 remote URL、`.git/config`、命令行参数或日志；
- 完成一次操作后清理临时凭据、临时仓库和工作区；
- 中止时向内置 Git 子进程传递终止信号，并对 Windows 文件锁导致的临时目录清理失败进行有限重试；
- 清理 `PATH`、`GIT_CONFIG_GLOBAL`、`GIT_CONFIG_SYSTEM`、`HOME` 等可能指向用户全局环境的变量；
- 显式设置 `GCM_CREDENTIAL_STORE=dpapi` 与项目内 `GCM_DPAPI_STORE_PATH`，阻止 Windows 默认 `wincredman` 存储介入；
- Git 运行失败时给出明确错误，不尝试使用电脑上的其它 Git 版本。

启动检查应显示内置 Git 版本、路径和健康状态。内置 Git 缺失、校验失败或版本不兼容时，只禁用共享仓库操作，不影响 B 站同步、RAG 和普通视频总结。Git 发行包的许可证和第三方声明必须随发布包提供。

同一文档再次上传时：

- 如果远程没有该 `documentId`，创建新增文件；
- 如果远程存在且哈希不同，生成更新 PR；
- 如果远程哈希相同，提示无需重复提交；
- 如果已有相同文档的未合并 PR，优先提示更新已有提交或等待旧 PR 处理，避免重复 PR。

## 6. GitHub Actions 与维护审核

### 6.1 PR 检查

共享仓库维护者应配置只读权限的 PR Action 检查；应用一键创建的个人仓库会自动预置等价检查：

- 所有修改是否位于当前贡献者目录；
- PR 作者的 GitHub 数字 ID 是否与目录一致；
- 是否只包含允许的 Markdown、图片和 JSON 文件；
- 是否存在 Cookie、API Key、数据库、脚本或明显的本地路径；
- `_star-owner-document.json` 与实际文件是否一致；
- SHA-256 是否匹配；
- Markdown 图片是否只引用当前文档目录内的资源；
- 单文件、单文档、单 PR 和仓库总大小是否在限制内；
- 同一贡献者、收藏夹和 BVID 是否出现不合法重复记录；
- JSON schema 和 `schemaVersion` 是否受支持；
- PR 不得修改 Actions、CODEOWNERS、PR 模板、仓库契约或其它维护配置；
- 禁止符号链接、未在元数据声明的文件、非标准目录，以及敏感/可执行/原始媒体文件。

Action 不执行上传文档中的任何脚本，也不向来自不可信 PR 的任务暴露仓库密钥。

贡献目录身份必须读取 PR 事件中的 `pull_request.user.id`。`github.actor`、workflow actor 和事件 `sender` 可能是触发重跑、代操作或自动化账户，不能作为文档顶层 GitHub 数字 ID 的授权依据。

### 6.2 主分支目录生成

当前应用优先依赖 GitHub Actions 生成的只读目录索引以降低请求数量。应用一次读取配置分支根目录 `catalog.json`，检查 schema、总数、重复路径、来源类型、远程路径边界、文件数量和声明总字节数；索引不可用时扫描 Git tree 与所有合法的 `_star-owner-document.json` 以兼容旧仓库。只有合并到目标分支的内容才会显示在面板中，未合并 PR 不会被导入。一键创建的仓库预置目录索引 Action，但索引不能取代导入阶段对文档元数据、实际文件路径、实际大小和 SHA-256 的校验。目录 Action 对同一仓库串行执行；推送发生竞争时必须重新拉取 main、再次校验并重建目录后有限重试，不能用强制推送覆盖其它提交。

既有自定义仓库可以由维护者增加只读目录索引 Action；索引必须由合并后的文档元数据生成，不能成为用户上传入口，也不能取代应用对文档路径和 SHA-256 的校验。一键创建的仓库已经采用以下根目录索引：

```text
catalog.json
```

`catalog.json` 不属于贡献者可直接修改的知识文档，只有目标分支上的目录索引 Action 可以根据已经合并的 `_star-owner-document.json` 重建它。

## 7. 下载者浏览与筛选

下载面板优先一次读取当前仓库配置分支根目录的 `catalog.json`，不读取未合并 PR，也不在浏览阶段下载整个仓库。私有仓库使用星藏家当前应用授权访问，不读取系统 Git 或浏览器凭据。支持按以下字段筛选：

- 贡献者；
- BVID；
- 标题和 UP 主；
- B 站账户；
- 远程收藏夹；
- 投稿时间、收藏时间和远程更新时间；
- 已导入、未导入、远程有更新、远程已失效。

列表中的时间规则：

- 有过更新时显示最后一次合并后的更新时间；
- 没有更新时显示首次合并/上传时间；
- GitHub 提交时间只作为辅助信息，文档元数据中的远程更新时间为主要显示值。

应用先读取并缓存元数据，再按用户选中的文档通过 GitHub Contents API 获取正文和资源。上传、Fork、分支和推送使用核心包内的 Portable Git；远程目录读取不回退到用户电脑上的全局 Git。

### 7.1 文档列表状态

每一行文档都应直观显示以下信息：

- 标题和 BVID；
- 贡献者 GitHub 显示名；
- B 站账户和远程收藏夹名称；
- 远程最近更新时间；
- 本地最近导入/同步时间；
- 当前共享收藏夹中的同步状态；
- 资源数量和文档大小。

状态以远程 `documentId`、远程元数据 blob SHA、本地来源绑定记录和本地文件哈希综合判断，不只检查文件是否存在。`1.4.0` beta1 当前面板显示的主要状态如下：

| 状态 | 含义 |
| --- | --- |
| 未下载 | 当前没有导入到任何本地共享收藏夹 |
| 已下载 | 当前目标共享收藏夹已有该文档，且内容为最新 |
| 已下载到其他收藏夹 | 本地其它共享收藏夹已有该文档，但当前目标收藏夹没有 |
| 已挂载 | 当前目标共享收藏夹已经挂载了该远程收藏夹 |
| 挂载有新增 | 已挂载的远程收藏夹出现了尚未导入的新文档 |
| 挂载有更新 | 已挂载的远程收藏夹中已有文档发生变化 |
| 需要更新 | 当前目标共享收藏夹已有该文档，但远程内容哈希已变化 |
| 下载中 | 当前正在下载或导入 |
| 更新中 | 当前正在更新本地副本 |
| 远程已失效 | 主分支目录已删除该文档，本地副本仍保留 |
| 本地已移除 | 用户删除过本地副本，默认不会自动恢复 |
| 本地已修改 | 本地文件哈希与上次导入内容不同，远程未变化时保留本地修改 |
| 同步冲突 | 本地和远程同时发生变化，保留本地文件并等待用户处理 |
| 导入失败 | 上次导入或更新失败，可重试 |

未选择目标共享收藏夹时，列表显示全局状态，例如“已下载到 2 个共享收藏夹”；选择目标收藏夹后，状态切换为相对于该收藏夹的状态，例如“已是最新”或“需要更新”。

列表支持按上述状态筛选，并提供行级操作：下载、更新、重试、查看来源、打开本地文档和查看远程目录。多选操作栏显示“待下载数量、待更新数量、远程失效数量和冲突数量”，避免用户在提交导入前误判状态。

远程时间和本地时间分开保存：

```text
remoteUpdatedAt  = 主分支中该 documentId 最后一次合并后的更新时间
lastImportedAt   = 本机成功导入该版本的时间
lastCheckedAt    = 本机最近一次检查远程目录的时间
```

## 7.2 下载前确认

用户选择目标共享收藏夹后，应用在确认区域汇总：

- 新下载文档数量；
- 需要更新文档数量；
- 已是最新文档数量；
- 已下载到其它共享收藏夹的文档数量；
- 远程已失效和本地冲突文档数量；
- 预计 Markdown、图片和元数据总大小。

下载者可以通过两种入口选择挂载范围，但本地挂载关系始终是同一种“远程收藏夹来源”：

- **选择文档范围**：只挂载用户明确选中的单篇或多篇文档，来源节点使用 `scope=documents`。后续只同步这些文档，不自动加入同一远程收藏夹中新出现的其它文档；
- **选择完整收藏夹范围**：将远程收藏夹当前全部合格文档挂载到目标共享收藏夹，来源节点使用 `scope=collection`。后续同步时自动导入该远程收藏夹新增的文档，并更新其中已挂载文档。

两种入口都先显示预计新增、更新、失效文档数量和总大小。下载者可以把多个不同贡献者或不同远程收藏夹挂载到同一个本地共享收藏夹中；不同远程收藏夹仍独立记录，不能依赖本地收藏夹名称判断来源。同一远程收藏夹只挂载一篇文档时就立即建立唯一来源节点，之后追加文档或切换为完整收藏夹范围都复用该节点，不存在先建立“单篇类型”再归并的步骤。

## 8. 导入到“共享”用户

### 8.1 共享用户与收藏夹

应用创建独立的系统用户类型：

```text
用户类型：shared
来源类型：remote-bilibili-summary
任务参与：否
RAG 可用：是
文档库可见：是
```

“共享”用户与“内置用户”和 B 站账户同级显示，但不出现在任务总览、任务激活、收藏夹同步或 Agent 工作流任务范围中。

下载者可以：

- 新建一个命名的共享收藏夹；
- 选择已有的共享收藏夹；
- 从一个或多个远程收藏夹中导入本次明确选择的文档；
- 将整个远程收藏夹目录设为自动跟随范围并挂载到该共享收藏夹。

共享收藏夹保存来源类型，并在创建后固定。第一版只允许 `remote-bilibili-summary` 文档进入共享收藏夹。若未来增加本地来源，必须使用不同来源类型的收藏夹，混合选择时弹窗提示并拒绝提交。

下载者可以把不同贡献者、不同远程收藏夹的 B 站总结放入同一个共享收藏夹，因为它们属于同一种来源类型；文档本身仍保存各自的远程来源信息。

本地共享收藏夹不是远程收藏夹的镜像目录，而是一个本地知识容器。它可以包含多个独立的远程收藏夹挂载；每个文档通过 `remoteOrigin` 和挂载 ID 追踪真实来源。远程收藏夹改名、贡献者改名或本地共享收藏夹改名，都不会改变同步身份。

同一个本地共享收藏夹可以同时挂载：

- 多个贡献者的远程 B 站收藏夹；
- 同一贡献者的多个远程 B 站收藏夹；
- 多个独立的 B 站单视频总结来源。

这些来源都属于 `remote-bilibili-summary` 类型，因此可以共存。身份判断不使用标题、文件名或本地收藏夹名称，而使用：

```text
贡献者 GitHub ID + B 站 UID + 远程来源收藏夹 ID/单视频命名空间 + BVID
```

同一 BV 出现在不同远程收藏夹或单视频命名空间中，会保留为不同文档。只有完全相同的远程来源身份才视为同一文档并执行更新。单视频命名空间固定带有 `single:` 前缀，不会与真实 B 站收藏夹 ID 冲突。

应用不会因重复操作在同一个本地共享收藏夹中复制产物：完全相同的远程收藏夹始终复用唯一来源节点，单篇、多篇和完整收藏夹操作只更新该节点的文档范围或自动跟随范围，任务只保留一份本地产物。不同远程收藏夹仍各自保留独立同步关系。

### 8.2 不自动去重

导入以远程 `documentId` 为单位，不以 BVID、标题或文件名去重。

因此以下内容可以同时存在：

- 不同贡献者对同一 BV 的总结；
- 同一贡献者在不同远程收藏夹中的同一 BV 总结；
- 不同模型、时间和提示词产生的不同总结。

列表和 RAG 检索结果必须展示贡献者、远程收藏夹、模型/生成时间等来源标签，避免用户误以为它们是同一版本。

## 9. 共享收藏夹更新

每个“本地共享收藏夹 + 共享仓库 + 远程收藏夹”组合保存一条唯一远程挂载来源记录。即使当前只选择一篇文档，也从第一次挂载起建立这条来源记录：

```json
{
  "id": "mount:<stable-id>",
  "scope": "documents|collection",
  "collectionId": "shared-collection-id",
  "repository": { "owner": "owner", "name": "repository", "branch": "main" },
  "remoteCollectionPrefix": "github-id/bilibili-user/remote-collection",
  "remotePaths": [".../_star-owner-document.json"],
  "lastSyncedAt": "...",
  "remoteFingerprint": "..."
}
```

`scope` 只控制同步范围，不表示两种挂载记录类型。`documents` 只同步 `remotePaths` 中明确选择的文档；`collection` 还会在后续同步时自动接收该远程收藏夹的新文档。每个导入文档同时保存对应来源绑定：

```json
{
  "repository": "owner/repository",
  "remoteCommitSha": "...",
  "mountId": "mount-...",
  "mountScope": "documents|collection",
  "contributorGithubId": "123456789",
  "bilibiliUid": "987654",
  "remoteCollectionId": "col-abcdef",
  "documentId": "doc-...",
  "remotePath": "123456/bilibili/col-abcdef/doc-0123456789abcdef/_star-owner-document.json",
  "lastImportedContentSha256": "...",
  "localContentSha256": "..."
}
```

点击共享收藏夹的“一键更新”时：

1. 优先一次获取主分支 `catalog.json`；索引不可用时回退一次 Git tree 兼容读取；
2. 按每个远程收藏夹来源节点分别检查当前同步范围；
3. 对 `scope=collection` 的来源，自动发现并导入远程收藏夹新增的合格文档；
4. 对 `scope=documents` 的来源，只检查已经明确选择的文档，不自动扩展范围；
5. 同一贡献者、同一来源、同一远程收藏夹、同一 BVID 的文档发生变化时更新；
6. 不同步同一用户的其它远程收藏夹，也不同步同名收藏夹中的同 BVID 文档；
7. 使用远程路径、正文哈希、元数据版本和本地文件完整性计算变化；未变化文档直接跳过并提示无需更新；
8. 远程删除文档时保留本地产物并标记“远程已失效”；
9. 下载失败、校验失败或部分中断时，不破坏原有本地文档。

同一挂载需要新增或更新多篇文档时，应用通过项目内置 Portable Git 对目标分支执行一次 partial clone，并用 sparse checkout 只提取选中的文档目录；不会为每篇文档重复读取 Git tree。若内置 Git 快照在回调开始前失败，兼容路径只获取一次 GitHub tree 并复用于这一批文档。目录索引提供每篇文件数与总字节数，应用还会复核快照实际大小，并在下载/导入前检查项目缓存盘和目标 Workspace 盘余量。

### 9.1 本地变更保护

共享文档默认按只读知识库处理。如果用户从文件系统手动修改了本地 Markdown：

- 更新前检测本地哈希；
- 本地未修改时允许自动更新；
- 本地已修改且远程也更新时标记“本地与远程冲突”，不静默覆盖；
- 用户可以选择保留本地副本、覆盖本地副本或另存为本地副本。

用户从文档库删除共享文档时，应用保留 `sharedExclusions` 删除标记，后续一键更新不自动把它重新下载回来。用户再次从远程目录明确挂载该文档时，删除标记才会清除并允许重新导入。手动删除本地产物后，同步也会标记为本地已删除并保持不自动恢复。

对于 `scope=collection` 的来源，用户删除单个本地文档后生成该共享收藏夹与远程路径对应的“本地排除”记录。后续同步仍会检查远程收藏夹，但不会反复把该文档加入本地。用户解除整个来源挂载时，只停止后续同步，不自动删除已经导入的本地产物。

远程删除后文档重新出现时，状态变为“远程已恢复”，仍需用户确认是否重新同步，不自动覆盖本地保留的内容。

## 10. 本地存储与 RAG/文档库隔离

共享文档导入后写入普通知识文档结构，并标记：

- 普通视频使用 `sourceType=shared-bilibili`，多 P 父/子文档使用 `sourceType=shared-bilibili-multipart-summary`；
- `collectionKind=shared`；
- `knowledgeActive=true`；
- `taskClaimable=false`；
- `sharedRemotePath`、`sharedRemoteSha`、`sharedMountIds` 和 `remoteState`。

RAG 助手可以读取 Markdown、图片和来源元数据，包括贡献者、BVID、投稿时间、收藏时间、远程更新时间和远程失效状态。当前项目使用关键词检索和运行时分块，因此导入后不需要同步向量数据库或 Embedding 文件。

共享文档删除、远程失效和同步冲突都不能创建 B 站待派发任务，也不能改变普通 B 站收藏夹的任务状态。

## 11. 网络、限流和事务安全

- 目录读取使用分页、缓存和条件请求，处理 GitHub API 限流；
- Git 操作前检查项目自带 Git 运行环境、版本和文件哈希；
- 网络失败时保留上次成功目录，并显示目录更新时间；
- 文档导入先写临时目录，哈希校验和资源校验完成后再提交数据库；
- 同一次挂载/同步使用收藏夹范围数据库快照和旧目录延迟清理；任一文档失败时整批恢复到操作前状态，不保留扩大后的 `remotePaths`、半成品任务或被部分覆盖的旧文档；
- 应用关闭或崩溃后可依据导入日志清理未完成临时文件；
- `schemaVersion` 不支持时拒绝导入并保留原有数据；
- 单次上传最多 1000 篇、总大小最多 1 GiB；单文件最多 25 MiB、单文档最多 256 个文件和 100 MiB；
- 上传过程中锁定应用交互并允许中止，GitHub 请求、Git 子进程、临时分支和临时目录按同一事务生命周期清理；
- 单次挂载/同步最多 2000 篇、实际内容最多 2 GiB，共享仓库总体积最多 10 GiB；单个文档和远程资源数量继续使用更小上限；
- 所有远程路径、文件名和 Markdown 图片引用都必须通过路径边界校验。

## 12. 维护与隐私规则

共享仓库需要提供贡献说明、内容许可说明和下架/纠错渠道。上传者确认后才能提交，应用不应默认公开用户的 B 站登录账号隐私字段。

仓库中禁止出现：

- Bilibili Cookie；
- AI 供应商 API Key；
- 应用 SQLite 数据库；
- 用户本地绝对路径；
- 原始视频和音频；
- 未经选择的过程日志和缓存。

## 13. 1.4.10 实现状态

1. 已定义共享元数据、稳定 `documentId`、远程文档目录和本地共享挂载记录；当前不使用全局 manifest。
2. 已打包并验证项目自带 Portable Git，所有 Fork 工作区、提交、推送均拒绝回退到用户全局 Git。
3. 已实现默认浏览器 GitHub 引导、Fine-grained Token 校验、加密存储和退出授权。
4. 已实现 B 站完成总结筛选、来源校验、文件预检、路径/大小/敏感内容保护。
5. 已实现可切换并持久化的公开/私有仓库；共享仓库的配置分支必须已有可克隆的初始提交。
6. 已实现一键创建公开个人共享仓库，并按实际默认分支预置 README、维护配置、PR 校验和目录索引 Actions。
7. 已实现按 GitHub 数字 ID 分流的主人临时分支 PR、普通贡献者 Fork/PR、提交作者身份以及上传中止清理。
8. 已实现一次最多 1000 篇/1 GiB、源文件流式复制、全应用独占进度层，以及候选/准备列表的独立筛选与批量移除。
9. 已实现 `catalog.json` 单次目录读取、配置分支 tree 兼容回退和共享用户导入；挂载从创建时即以“本地共享收藏夹 + 共享仓库 + 远程收藏夹”为唯一来源，单篇、多篇与整收藏夹只改变同一来源的文档范围或自动跟随模式。
10. 已实现内置 Git partial clone + sparse checkout、本地快照批量导入、API 单 tree 回退、远程收藏夹来源级增量同步、无变化跳过、远程新增、远程删除标记、本地删除排除、冲突保护和整批回滚；同批多个远程收藏夹共用一次受限仓库快照。
11. 已接入文档库、关键词 RAG、多 P 父文档与 P 子文档的导入关系。
12. 已加入空仓库、私有授权读取、网络/中止、重复挂载、跨仓库多 P 隔离、路径安全、仓库文件白名单、来源伪造、权限边界和迁移测试。
13. 已修复长文件名最终总结被 Action 误判缺少 `summary.md` 的契约错位；单视频和多 P 上传统一生成规范入口，排除 `agent-draft` 与 ASR/字幕/评论过程文件，并测试 Action 对正文和资源 SHA-256 的校验。
14. 本地多 P `index.md` 会确定性提取每个已完成子文档的 `## 小结`，写入“每 P 小结”并保留完整子文档入口；旧父目录在应用启动时自动重建，不新增模型请求。
15. 自定义仓库必须通过 `_star-owner-repository.json` 的仓库身份、默认分支、schema 与能力校验后才会保存；已验证仓库进入持久下拉列表，进入工具时复查当前仓库，失败只更新健康状态而不覆盖连接。
16. 远程目录按“GitHub 用户 -> B站用户 -> 收藏夹 -> 视频总结”分层，并支持 GitHub 用户名、B站用户名和视频名称独立搜索；可挂载筛选结果、整个收藏夹或单篇文档，目录视口可拖动增高。
17. 本地挂载按“本地共享收藏夹 -> 远程收藏夹挂载源 -> 已挂载文档”三层树展示；只挂载一篇文档时仍创建对应远程收藏夹来源节点。挂载源支持多选批量同步且按仓库复用目录与快照，创建/检测仓库、目录读取、挂载和同步统一报告项目数、当前条目及百分比进度。
18. 共享收藏夹只对文档库、知识 API 和 RAG 可见；任务总览与 Agent 收藏夹选择器排除该类型，Store 启用接口、单视频入口和 Agent 创建/领取后端同时拒绝共享任务。
19. 旧版把多个远程收藏夹压在同一条文档挂载记录中的数据，会在管理器初始化时按远程收藏夹拆分并重新绑定 `sharedMountIds`；迁移不重复下载或复制本地文档。
20. PR Action 使用 `pull_request.user.id` 约束贡献目录；目录 Action 串行运行，并在 main 竞争更新时重新拉取、校验、构建和重试。默认共享仓库契约标记、19 篇目录及两条线上 Action 已验证。

切换到既有自定义仓库不会擅自改写该仓库的 Actions 或维护文件；只有用户明确点击“一键创建个人共享仓库”时，应用才会在新仓库内写入本协议附带的模板。

## 14. 多 P 视频工具与共享扩展

### 14.0 多 P 视频总结工具入口与设置

“B站之外”提供独立的“多 P 视频总结”工具卡片。它与“视频总结（单个）”和 Agent 视频总结工作流完全分开，不能复用这两个入口的任务派发路径。

用户输入或粘贴 BVID 后，应用读取视频页面列表并显示：

- P 序号；
- 每 P 的页面标题；
- `cid`；
- 时长和可用性；
- 已有本地总结状态。

用户可以勾选需要总结的 P 范围。无效、失效或特殊类型的 P 不可选，并显示原因。视频必须是标准 Bilibili 多 P 视频；单 P、番剧、电影、纪录片、综艺、互动视频和其它特殊视频不能通过此工具绕过现有保护逻辑。

开始前必须新建或选择一个 `B站多P类型` 内置收藏夹。相同 BVID 在目标收藏夹中已经有部分或全部 P 产物时，弹窗列出已完成的 P，并要求用户选择继续未完成范围、重新覆盖指定 P 或取消。

本次父任务的设置一次性保存并应用到所有选中的 P：

- AI 供应商和模型；
- 补充要求；
- 多 P 内部逻辑并发数，默认 2；
- 每 P 关键帧最低数量，默认 8；
- 关键帧时间间隔；
- ASR 是否启用、使用的 ASR 模型和 GPU/CPU 通道；
- 是否保留下载视频、ASR 字幕、站内字幕和其它过程产物。

进入页面时重新从后端读取可用供应商和模型，不能使用过期的 Renderer 模型列表。设置在父任务创建时生成不可变快照，后续继续任务时明确显示本次使用的设置；重新覆盖 P 时才允许重新选择设置。

父任务内部为每个 P 创建独立子任务，子任务复用应用统一的下载、FFmpeg、ASR、字幕、关键帧、AI 和产物验收资源池。用户设置的并发数只是该父任务的并发上限，实际并发仍受全局资源调度器、ASR 通道、模型供应商和硬盘容量限制。

父任务总体进度以选中 P 的完成情况和当前子任务阶段进度计算；界面同时展示父任务总进度、每个 P 的阶段进度、当前运行数、排队数和失败数。当前父任务结束、停止或进入可恢复的部分完成状态后，用户才能配置下一个多 P 视频。

### 14.1 多 P 父任务查看器

“B站之外”中的多 P 工具只允许同时存在一个正在处理的父任务。父任务内部可以按用户设置启动多个逻辑 P 工作流，默认并发数为 2；实际资源使用仍由应用统一调度器限制。

多 P 工具只接受标准 Bilibili 多 P 视频。视频总结（单个）和 Agent 视频总结工作流继续明确拒绝 `pages.length > 1` 的多 P 视频，以及番剧、电影、纪录片、综艺、互动视频等特殊视频类型；这些入口不能因为多 P 工具存在而自动回退或改走多 P 处理逻辑。多 P 工具本身也拒绝特殊视频。

用户选择一个 `B站多P类型` 收藏夹后，面板展示该收藏夹下的父任务列表。每个父任务显示：

- BVID、标题和总 P 数；
- 已完成 P 数、失败 P 数、未完成 P 数；
- 父任务总体进度条；
- 处理状态、创建时间、最近更新时间和中断原因；
- 使用的供应商/模型和关键处理设置摘要。

父任务可展开查看每个 P：

- P 序号、B 站页面标题和 `cid`；
- 当前状态和单 P 进度；
- 产物是否存在；
- 失败原因或中断原因；
- 是否已保留视频、ASR、字幕等过程文件。

### 14.2 继续和删除

父任务可以直接执行“继续”。继续前弹出 P 范围选择列表，提供醒目的“全选”操作，并显示：

- 未完成 P：默认可直接继续；
- 已完成 P：若再次选择，将明确标注“重新总结并覆盖”；
- 已失效或缺少 `cid` 的 P：不可选并显示原因。

当前已有任何多 P 父任务处于运行、停止中或恢复处理中时：

- 不允许启动新的多 P 父任务；
- 不允许继续其它父任务；
- 仍可以查看列表和展开状态；
- 当前任务停止并完成清理后，才恢复启动和继续按钮。

删除父任务前必须确认。删除会清理父目录、目录 Markdown、所有 P 子文档、资源、过程缓存、数据库父子记录和挂载前的本地状态；不会把任何任务放回 B 站任务列表，也不会影响远程共享仓库。运行中的父任务必须先停止，不能直接删除。

### 14.3 多 P 产物结构

多 P 收藏夹中的每个父任务使用以下结构：

```text
B站多P类型收藏夹/
  <safe-bvid-or-parent-id>/
    index.md
    metadata.json
    parts/
      cid-<cid>/
        summary.md
        assets/
```

`index.md` 是文档库唯一展示条目，记录父视频信息、每 P 标题、状态、完成时间和子文档入口，并直接包含每个已完成 P 的 `## 小结`。点击目录中的 P 后，单独弹窗打开对应的完整 `summary.md`。

父任务和 P 子任务分别持久化。父任务状态可以是“未开始、处理中、已完成、部分完成、已中断、部分失败、已删除”；P 子任务状态独立记录。单 P 失败默认不销毁其它已完成 P，父任务最终可以进入“部分失败”，之后只重试失败 P。

目录 Markdown 在每个 P 完成后都可以安全更新，标记当前父任务进度，并从对应最终 Markdown 确定性提取 `## 小结`；相对图片和资源路径会改写到父目录视角。这样应用崩溃或手动中断后，用户仍能直接看到已完成部分的结论。该过程不额外调用 AI，升级后的既有父目录会在应用首次启动时自动重建；未来如需重新生成整部视频的综合总览，可作为单独的可选步骤。

### 14.4 多 P 总结的共享方式

多 P 父任务作为一个完整共享文档包上传，不能只把某个 P 当作普通单视频文档上传。上传包包括：

- `index.md`；
- 已完成 P 的 `summary.md`；
- Markdown 引用的图片资源；
- 父任务和 P 的脱敏元数据；
- `_star-owner-document.json` 中的内容哈希、资源哈希和完成状态。

默认只有全部选定 P 已完成、父目录已正常生成的父任务才出现在上传候选列表。部分完成父任务默认只保留在本地，用户继续完成后再上传。未来如果需要共享部分结果，可以增加明确的“允许上传部分完成父任务”选项，并在远程目录显示部分完成状态。

多 P 父任务使用独立的文档类型和稳定身份：

```text
documentType = bilibili-multipart-summary
parentDocumentId = sha256(
  "v1|github:" + contributorGithubNumericId +
  "|bilibili:" + bilibiliUid +
  "|collection:" + stableMultipartCollectionId +
  "|bvid:" + normalizedBvid
)
partId = cid
```

`stableMultipartCollectionId` 必须由来源 B 站 UID、内置多 P 收藏夹稳定 ID 和 `bilibili-multipart` 来源类型共同确定。它用于防止同一贡献者的多个 B 站账户或多个内置多 P 收藏夹发生串档，但不需要在目录中建立 B 站 UID 层级。

目录结构扩展为：

```text
<github-numeric-id>/
  multipart/
    col-<source-collection-hash>/
      doc-<stable-document-hash>/
        index.md
        metadata.json
        _star-owner-document.json
        parts/
          cid-123/
            summary.md
            assets/
```

`col-<source-collection-hash>` 和 `doc-<stable-document-hash>` 不使用 B 站 UID 目录层级；稳定来源 ID 内部包含 B 站 UID、实际内置多 P 收藏夹稳定 ID和来源类型，B 站账户名、UID 和收藏夹名称只保存在 `_star-owner-document.json` 和其它脱敏元数据中。单视频使用同样的贡献者直挂原则，目录为 `<github-numeric-id>/single/col-<hash>/doc-<hash>/`。

单 P 和多 P 都属于远程 B 站来源，因此可以挂载到同一个本地“共享”收藏夹；本地列表仍按 `documentType` 显示“单视频”或“多 P 父任务”标签。多 P 父任务不能与本地文档来源混入同一个共享收藏夹。

### 14.5 多 P 挂载与增量同步

挂载多 P 父任务时，下载者挂载的是父任务目录，不是若干互不相关的 P 文件。挂载后的本地共享收藏夹只展示一个父任务目录项，展开后查看各 P。

如果挂载的是整个远程多 P 收藏夹：

- 远程新增父任务会在同步时进入本地；
- 已挂载父任务新增完成 P 时，只导入新增 P 并更新目录；
- 已有 P 内容变化时，按 `parentDocumentId + partId` 更新对应 P；
- 远程删除某个 P 或父任务时，本地保留产物并标记远程失效；
- 本地删除某个 P 后记录挂载级本地排除状态，不会在下次同步中反复恢复。

如果只选择单个多 P 父任务，则对应远程收藏夹来源使用 `scope=documents`，只同步该父任务，不自动扩展到同一远程收藏夹的其它父任务。多 P 的共享更新仍遵循现有范围规则：`scope=collection` 自动接收新增内容，`scope=documents` 不自动扩大范围。

### 14.6 RAG 与多 P 文档

这是多 P 功能必须重点实现和测试的部分。文档库只展示父任务的 `index.md`，但 RAG 检索不能只读取目录元数据，必须能够继续读取 `parts/<cid>/summary.md`。

RAG 应支持以下能力：

- 目录检索结果显示父视频、BVID、P 标题、`cid`、贡献者和来源收藏夹；
- 用户询问某个 P 时，可以按 `parentDocumentId + partId/cid` 精确读取对应 P 的完整 Markdown；
- 用户询问整部多 P 视频时，可以跨多个 P 检索并按 P 分组返回证据；
- P 文档中的本地图片可以继续通过多模态工具读取和输出；
- 同一 BV 的不同父任务、不同贡献者或不同来源收藏夹不互相串上下文；
- RAG 结果和引用显示父任务与 P 的来源标签，避免多个 P 或多个总结版本混淆。

实现上将父任务作为一个可见知识文档，将 P 子文档作为受父任务约束的隐藏子文档；关键词检索时遍历父目录和子文档，读取工具通过父 ID 和 `cid` 做边界校验。`index.md` 只汇总各 P 的短小结，不复制思维导图、正文、字幕和处理记录；完整内容继续只保留在子文档，避免目录展示和 RAG 上下文过度重复。

共享多 P 文档使用 `sourceType=shared-bilibili-multipart-summary`、`collectionKind=shared`、`taskClaimable=false`，可以进入 RAG 和文档库，但不进入任务总览、B 站同步或 Agent 工作流。

### 14.7 重新读取 P 信息与追加处理

多 P 视频发布新 P 后，不能要求用户重新创建一个父任务。多 P 工具和父任务查看器都提供“重新读取 P 信息/检查新增 P”操作。该操作只重新读取 Bilibili 页面列表和页面元数据，不自动下载或总结视频。

父任务身份由来源收藏夹稳定 ID、BVID 和来源类型确定，不包含总 P 数；P 子任务使用 `cid` 作为稳定的 `partId`，不能使用当前 P 序号、标题或时长作为唯一身份。刷新结果按 `cid` 对比：

- 新出现的 `cid`：创建新的“待派发” P 子任务，默认选中，用户确认后可以追加到原父任务继续处理；
- 已存在的 `cid`：保留原有状态、产物、失败记录和用户选择，只更新 P 序号、标题、时长、可用性等最新元数据，不自动重复总结；
- 原列表中消失的 `cid`：标记为“B站页面已移除/暂不可用”，保留已有产物和历史记录，不删除文件，也不重新创建任务；
- 已存在但标题、时长或可用性变化的 `cid`：提示元数据发生变化；用户明确选择覆盖时才重新处理该 P。

刷新后弹窗显示旧 P 数、新 P 数、新增列表、移除列表和元数据变化。用户可以全选新增 P，也可以手动选择新增 P；只有确认后才更新父任务快照、目录 Markdown、总 P 数和进度。继续追加任务仍遵守“同一时间只能有一个多 P 父任务处于运行、停止中或恢复处理中”的限制，且复用原父任务保存的模型、ASR、关键帧、字幕、缓存和并发设置快照。

如果原父任务已经完成并上传共享仓库，追加 P 会使本地父任务进入“有新增待处理 P/待更新共享包”状态。新增 P 全部完成后，使用原 `parentDocumentId` 更新同一个共享包、目录和 manifest，不创建第二个同 BVID 的父任务；已经挂载该父任务的下载者在同步时只接收新增或发生变化的 P。中断时仍只清理当前 P 的过渡产物，原来已完成的 P 和新完成的 P 都保留。

### 14.8 外部 Agent 知识库接口与提示词同步

外部 Agent 知识库接口、启动页中的可复制提示词示例和项目文档属于同一个对外协议。以后每次新增、删除或修改接口、字段、筛选语义、分页规则、错误码、文档类型或多 P 父子文档读取方式，必须在同一个变更中同步更新：

- `DESIGN.md` 的协议设计；
- `README.md`、`DEPLOYMENT.md` 的接口说明；
- `AGENTS.md` 的外部 Agent 接入提示词示例；
- 启动页展示的默认提示词和相关 UI 说明；
- 知识库 API 的协议测试、兼容性测试和错误恢复示例。

提示词示例至少要求外部 Agent：先读取 `/api/manifest`，再读取目录和元数据后筛选文档；对多 P 文档先读取父任务目录，再按 `parentDocumentId + partId/cid` 获取对应 P 内容；回答时区分父任务、P 序号、P 标题、贡献者和来源收藏夹；不能把不同父任务、不同贡献者或不同收藏夹下的相同 BVID 当成同一份总结。提示词应明确只读边界、分页读取、图片资产读取、引用来源和 `404/409/410/413/416` 等错误恢复方式。

接口字段和示例提示词必须保持向后兼容。若协议版本发生不兼容变化，先更新 `/api/manifest` 的协议版本和能力声明，再同时更新提示词、文档和测试；不能只修改后端接口而保留旧示例。

## 15. 1.5.0 共享工具界面契约

共享仓库配置默认收起。收起状态只显示当前已连接仓库的摘要；仓库切换、连接、检测和创建配置仅在用户展开后显示。下载挂载与上传提交两栏使用相同的标题、说明、主题色顶部边框和纵向流程轨道，远程/本地以及候选/准备四组列表保持对应尺寸；用户主动调整远程列表高度时，上传候选列表同步对齐。接近最小窗口宽度时仍按既有响应式规则改为单列，标题、操作和方向提示不得重叠。

这些界面约束不改变仓库注册表、验证状态、挂载来源、上传事务、稳定文档 ID 或数据库结构。Renderer 可以维护折叠状态和列表尺寸，但不能在布局代码中重写仓库身份、分支、选择集或共享任务绑定。
