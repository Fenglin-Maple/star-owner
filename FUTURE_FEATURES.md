# 未来功能备忘录

> 状态：候选功能，尚未排期，当前版本均未实现。
>
> 本文用于保存产品方向和后续讨论结论，不构成现有功能契约。任何功能进入开发前，都应先补充完整设计、权限边界、数据迁移、失败恢复和测试方案；功能设计发生变化时同步更新本文。进入正式实现阶段后，应为复杂模块建立独立设计文档，并同步更新 README、DESIGN、SECURITY、DEPLOYMENT、外部 Agent 提示词和相关自动化测试。

## 共同原则

- 新能力应模块化接入，避免把插件、工具或 AI 文件操作逻辑直接耦合进现有 Bilibili 同步、视频总结、RAG 和共享文档主流程。
- 所有长时间操作都应支持进度展示、快速取消、超时、失败回滚和再次执行；不能让某个工具调用永久占用 RAG 会话或应用资源。
- 文件、网络、代码执行、MCP 和第三方工具必须采用最小权限。默认不允许越过受管 Workspace、读取凭证、修改应用源码、调用用户全局运行时或执行未经授权的系统命令。
- 继续使用项目自身提供的 Node、Python、FFmpeg、Git 等运行环境。模块若需要新增依赖，必须声明版本、许可证、体积、平台和安装/卸载边界。
- AI 供应商能力需要显式探测。视觉、工具调用、长上下文或结构化输出不受支持时，应清楚提示，不能盲目重试。
- 所有新增知识来源都要有稳定 ID、来源元数据、资产边界、删除与重建规则，并能被 RAG、文档库、更新迁移和备份逻辑一致识别。

## 1. RAG 助手笔记

### 已明确设想

- 用户可在 RAG 对话过程中随时创建笔记，并可继续编辑已有笔记。
- 笔记采用 Markdown 格式，支持文字、图片及其它适合 Markdown 管理的内容。
- 笔记可作为独立知识来源加入知识库，供 RAG 助手在用户选择后检索和引用。
- 笔记需要在文档库中可识别、查看和管理，不能与视频总结产物混淆。

### 实现前需完善

- 明确笔记的用户、笔记本/收藏夹、会话和知识库之间的归属关系，以及稳定 ID 和重命名规则。
- 设计图片等附件的受管存储、相对路径、复制/导出、删除、缺失检测和迁移规则。
- 决定自动保存、草稿恢复、编辑冲突、历史版本和撤销能力的范围。
- 区分用户手动写入、AI 建议写入和 AI 直接修改；涉及覆盖或删除时提供明确确认与恢复手段。

## 2. RAG 助手按需查看互联网图片

### 已明确设想

- 网页正文读取仍以 DOM 文本和链接为主，不在打开网页时批量下载全部图片。
- RAG 助手读取网页后，可按需调用独立图片接口访问 DOM 中发现的互联网图片地址。
- 图片工具应把图片作为多模态图片内容发送给支持视觉的模型，而不是把 Base64 混入普通文字段落。
- 图片读取必须可快速取消；失败不能阻塞当前回复，停止生成后同一会话仍可继续使用。

### 实现前需完善

- 延续现有网页安全策略，限制协议、重定向、私有网络、DNS 重绑定、响应类型、文件大小、像素尺寸和下载时间。
- 明确单批图片数量、单轮累计策略、缓存期限和重复图片去重方式，并结合供应商上下文预算动态控制。
- 对不支持视觉、拒绝远程图片或采用特殊多模态协议的供应商提供能力探测与兼容提示。
- 工具结果应记录原始页面、图片 URL、内容类型和失败原因，但不能泄露本地路径、凭证或敏感请求头。

## 3. RAG 助手文件、代码与 Skill 工作能力

### 已明确设想

- RAG 助手可创建和编辑文档、代码文件与 Skill。
- RAG 助手可按权限调用文件和 Skill，并使用项目受控运行时执行代码。
- 相关能力应作为显式工具接口提供，而不是让模型绕过应用边界直接访问本机。

### 实现前需完善

- 建立独立工作区和细粒度权限：读取、创建、覆盖、删除、执行、联网和访问知识库应分别授权。
- 默认禁止访问应用凭证、Cookie、数据库、运行时内部文件、用户全局 Git/Node/Python 配置和 Workspace 之外的路径。
- 文件修改应提供差异预览、确认、原子写入、备份与撤销；代码执行需要超时、输出上限、资源限制、取消和子进程清理。
- Skill 需要定义清单、版本、依赖、入口、权限、来源和卸载契约；安装的 Skill 与应用内置 Skill 应明确区分。
- 明确会话级授权和持久授权边界，避免一次同意被无限扩大为后续所有操作权限。

## 4. 应用内 MCP 管理栏目

### 已明确设想

- 增加独立 MCP 栏目，以模块化方式为应用内 AI 提供 MCP 接口。
- 支持查看、启用、停用和管理已安装 MCP。
- 支持从 GitHub 下载符合应用规范的 MCP 功能模块，并可卸载或删除。

### 实现前需完善

- 定义星藏家 MCP 包清单，包括模块 ID、版本、入口、支持平台、权限、依赖、配置项、校验值和许可证。
- 安装前展示来源和权限，区分受信任官方模块与第三方模块；考虑固定提交/版本、SHA-256 校验和更新回滚。
- MCP 进程必须使用受控环境、独立配置和最小权限，不能继承供应商密钥、GitHub 令牌或用户全局 PATH。
- MCP 工具能力应按会话和模型暴露，处理命名冲突、不可用服务、异常退出、超时、取消和日志脱敏。
- 卸载应清理模块自身文件与受管配置，但保留或删除用户数据必须单独确认，绝不能影响其它模块或应用主数据。

## 5. “B站之外”可安装工具生态

### 已明确设想

- 建立独立的 GitHub“星藏家工具仓库”，发布可被应用识别的模块化工具。
- 用户可在“B站之外”栏目浏览、下载、安装、更新和卸载工具；安装成功后以工具卡片加入该栏目。
- 候选工具包括但不限于：
  - “小黑盒”用户收藏夹文章一键加工为知识库；
  - 知乎用户收藏夹内容批量加工为 Markdown 知识库；
  - `image2`、Nano Banana 等图片生成工具；
  - 后续其它适合本地知识加工或内容生产的实用工具。

### 实现前需完善

- 定义工具包规范：稳定 ID、版本、入口页面、后端入口、权限、运行时依赖、数据目录、图标、进度事件、取消协议、升级和卸载脚本。
- 工具仓库应提供可一次读取的签名/校验目录，应用验证仓库标识、包结构、版本兼容性和 SHA-256 后才能安装。
- 第三方站点导入工具需分别处理登录授权、服务条款、速率限制、隐私、增量同步、重复内容和失效内容，不能复用或导出应用已有 Cookie。
- 知识加工工具产生的 Markdown、图片、索引和元数据必须符合统一知识库契约，可被文档库、RAG、迁移与删除流程正常管理。
- 图片生成等消耗型工具应明确供应商、模型、费用、输出目录、内容安全和失败重试，不得默认占用视频工作流资源。
- 工具执行需要接入统一资源调度，但应拥有独立模块边界；单个第三方工具故障不能拖慢或破坏主应用。

## 后续维护

- 本文只记录候选方向，不在应用 UI 或发布说明中宣称为已实现功能。
- 每次形成新的产品决定，应将“待完善”条目转化为明确结论；存在冲突时以用户最新确认的设计为准。
- 功能开始实现时，先完成威胁模型、数据结构、迁移策略、模块接口、UI 流程和测试矩阵，再进入代码修改。
- 功能完成后，在本文标注实现版本并链接对应正式设计文档；不直接删除历史决策背景。
